// Custom generator output (see prisma/schema.prisma) — not the shared @prisma/client path.
import { PrismaClient } from '../../node_modules/.prisma-client-relay/index.js'

export const prisma = new PrismaClient()
